<Window x:Class="WardLock.Views.AutoTypePickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Type Code" Width="300" SizeToContent="Height" MaxHeight="360"
        WindowStartupLocation="Manual" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ShowActivated="True"
        Deactivated="OnDeactivated" KeyDown="OnWindowKeyDown">

    <Border Background="#1e1e2e" CornerRadius="8" BorderBrush="#45475a" BorderThickness="1">
        <StackPanel Margin="10">
            <TextBlock Text="Type code into previous window" FontSize="11"
                       Foreground="#a6adc8" Margin="2,0,2,6"/>
            <TextBox x:Name="FilterBox" FontSize="13" Padding="6,4" Margin="0,0,0,6"
                     Background="#313244" Foreground="#cdd6f4" BorderBrush="#45475a"
                     CaretBrush="#cdd6f4" TextChanged="OnFilterChanged"
                     PreviewKeyDown="OnFilterKeyDown"/>
            <ListBox x:Name="AccountList" MaxHeight="260"
                     Background="Transparent" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     HorizontalContentAlignment="Stretch"
                     MouseDoubleClick="OnItemDoubleClick">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="8,6"/>
                        <Setter Property="Foreground" Value="#cdd6f4"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Bg" Background="Transparent" CornerRadius="4"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bg" Property="Background" Value="#45475a"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bg" Property="Background" Value="#313244"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <DockPanel>
                            <TextBlock DockPanel.Dock="Right" Text="{Binding SourceLabel}"
                                       FontSize="9" Foreground="#585b70" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding DisplayName}" FontSize="13"
                                       TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                        </DockPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <TextBlock Text="Enter — type code   |   Esc — cancel" FontSize="9"
                       Foreground="#585b70" Margin="2,6,2,0" HorizontalAlignment="Center"/>
        </StackPanel>
    </Border>
</Window>
